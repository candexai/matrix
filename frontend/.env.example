NEXT_PUBLIC_API_URL=http://localhost:5001/api/v1

# ---- Auth ----
JWT_SECRET=change-me-long-random

# Optional: branded inbound SIP host shown in Phone Numbers → Connect SIP trunk (leave empty to hide the address)
NEXT_PUBLIC_SIP_INBOUND_HOST=
